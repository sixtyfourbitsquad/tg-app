.next
node_modules
.git
.gitignore
.env
.env.local
.env.*.local
secrets/
logs/
dist-seed/
*.log
npm-debug.log*
README.md
TEST_GUIDE.md
TEST_CHECKLIST.md
Dockerfile
docker-compose.yml
.dockerignore
scripts/
# bulk-upload runs *inside* the container (see Dockerfile runner stage),
# so we re-include it after ignoring the rest of scripts/. The PC-side
# helpers (deploy-videos.sh, download-tg-channel.py) stay out of the image.
!scripts/bulk-upload.cjs
